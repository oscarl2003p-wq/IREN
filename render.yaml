services:
  - type: web
    name: iren-backend
    runtime: node
    buildCommand: npm install && npm run build -w apps/backend
    startCommand: node apps/backend/dist/main.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
